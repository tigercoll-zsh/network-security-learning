---
title: Day079：红蓝对抗 - 安全加固 Checklist
tags:
  - 网络安全
  - 渗透测试
  - 红蓝对抗
categories:
  - 90天安全突击
stage: 实战演练
abbrlink: ef59f
date: 2026-01-28
updated: 2026-01-28
---

# Day079：红蓝对抗 - 安全加固 Checklist

## 📋 基本信息

| 项目 | 内容 |
|------|------|
| **所属阶段** | 🏆 实战演练（第三阶段） |
| **所属周次** | Week 13：红蓝对抗与防御 |
| **今日主题** | 安全加固 Checklist |
| **预计时间** | 3-4 小时 |
| **难度等级** | ⭐⭐⭐⭐ 中高级 |

---

## 🔥 今日痛点场景

> **场景**：渗透测试报告提交后，客户问你：「那我们应该怎么加固？」你需要给出具体可执行的加固建议。
>
> **问题**：如何系统性地加固一个系统？从哪里开始？
>
> **今天的任务**：掌握系统安全加固的方法论和实操清单。

---

## 🎯 今日目标

- [ ] 掌握 Windows 安全加固要点
- [ ] 掌握 Linux 安全加固要点
- [ ] 了解网络和应用层加固
- [ ] 形成可复用的加固清单

**闯关条件**：能够独立完成一台服务器的基本安全加固。

---

## ⚔️ 实战任务（先动手！）

### 任务 1：Windows 安全加固（60 分钟）

**账户安全**：

```cmd
:: 禁用 Guest 账户
net user Guest /active:no

:: 重命名 Administrator 账户
wmic useraccount where name='Administrator' rename 'SecAdmin'

:: 设置密码策略
net accounts /minpwlen:12 /maxpwage:90 /minpwage:1 /uniquepw:5

:: 设置账户锁定策略
net accounts /lockoutthreshold:5 /lockoutduration:30 /lockoutwindow:30
```

**服务加固**：

```cmd
:: 禁用不必要的服务
sc config RemoteRegistry start= disabled
sc config Spooler start= disabled
sc config SNMP start= disabled

:: 查看正在运行的服务
net start
```

**审计策略**：

```cmd
:: 开启审计策略
auditpol /set /subcategory:"Logon" /success:enable /failure:enable
auditpol /set /subcategory:"Account Lockout" /success:enable /failure:enable
auditpol /set /subcategory:"Process Creation" /success:enable /failure:enable
auditpol /set /subcategory:"Privilege Use" /success:enable /failure:enable

:: 查看审计策略
auditpol /get /category:*
```

**防火墙配置**：

```cmd
:: 启用防火墙
netsh advfirewall set allprofiles state on

:: 阻止入站连接（默认）
netsh advfirewall set allprofiles firewallpolicy blockinbound,allowoutbound

:: 只允许特定端口
netsh advfirewall firewall add rule name="Allow RDP" dir=in action=allow protocol=TCP localport=3389 remoteip=192.168.1.0/24
```

**Windows 安全加固清单**：

| 类别 | 检查项 | 状态 |
|------|--------|------|
| 账户 | Guest 账户禁用 | ⬜ |
| 账户 | Administrator 重命名 | ⬜ |
| 账户 | 密码策略配置 | ⬜ |
| 账户 | 账户锁定策略 | ⬜ |
| 服务 | 禁用不必要服务 | ⬜ |
| 服务 | 自动更新启用 | ⬜ |
| 网络 | 防火墙启用 | ⬜ |
| 网络 | 关闭不必要端口 | ⬜ |
| 审计 | 登录审计启用 | ⬜ |
| 审计 | 进程审计启用 | ⬜ |

---

### 任务 2：Linux 安全加固（60 分钟）

**账户安全**：

```bash
# 设置密码策略（/etc/login.defs）
PASS_MAX_DAYS   90
PASS_MIN_DAYS   1
PASS_MIN_LEN    12
PASS_WARN_AGE   7

# 锁定空密码账户
awk -F: '($2 == "") {print $1}' /etc/shadow

# 禁用 root SSH 登录
# 编辑 /etc/ssh/sshd_config
PermitRootLogin no

# 只允许特定用户 SSH
AllowUsers admin operator
```

**SSH 加固**：

```bash
# 编辑 /etc/ssh/sshd_config
Port 22222                    # 修改默认端口
PermitRootLogin no            # 禁止 root 登录
PasswordAuthentication no     # 禁用密码，只用密钥
MaxAuthTries 3                # 最大尝试次数
ClientAliveInterval 300       # 空闲超时
ClientAliveCountMax 2
Protocol 2                    # 只使用 SSH2

# 重启 SSH
systemctl restart sshd
```

**防火墙配置**：

```bash
# 使用 iptables
iptables -P INPUT DROP
iptables -P FORWARD DROP
iptables -P OUTPUT ACCEPT
iptables -A INPUT -m state --state ESTABLISHED,RELATED -j ACCEPT
iptables -A INPUT -i lo -j ACCEPT
iptables -A INPUT -p tcp --dport 22222 -j ACCEPT
iptables -A INPUT -p tcp --dport 80 -j ACCEPT
iptables -A INPUT -p tcp --dport 443 -j ACCEPT

# 保存规则
iptables-save > /etc/iptables.rules

# 或使用 ufw
ufw default deny incoming
ufw default allow outgoing
ufw allow 22222/tcp
ufw enable
```

**文件权限**：

```bash
# 关键文件权限检查
chmod 600 /etc/shadow
chmod 644 /etc/passwd
chmod 700 /root
chmod 755 /home/*

# 查找 SUID 文件
find / -perm -4000 -type f 2>/dev/null

# 查找 world-writable 文件
find / -perm -o+w -type f 2>/dev/null
```

**Linux 安全加固清单**：

| 类别 | 检查项 | 状态 |
|------|--------|------|
| 账户 | 密码策略配置 | ⬜ |
| 账户 | 空密码账户检查 | ⬜ |
| SSH | 禁止 root 登录 | ⬜ |
| SSH | 修改默认端口 | ⬜ |
| SSH | 密钥认证 | ⬜ |
| 防火墙 | iptables/ufw 配置 | ⬜ |
| 权限 | 关键文件权限 | ⬜ |
| 权限 | SUID 文件审计 | ⬜ |
| 更新 | 系统更新 | ⬜ |
| 日志 | 日志服务运行 | ⬜ |

---

### 任务 3：网络层加固（30 分钟）

**网络架构加固**：

| 措施 | 说明 |
|------|------|
| 网络分段 | DMZ、内网、办公网分离 |
| 访问控制 | ACL 最小权限原则 |
| 边界防护 | 防火墙、IDS/IPS 部署 |
| VPN 接入 | 远程访问走 VPN |
| 流量监控 | 全流量记录分析 |

**常见配置**：

```bash
# 禁用 IP 转发（非路由器）
echo 0 > /proc/sys/net/ipv4/ip_forward

# 禁用 ICMP 重定向
echo 0 > /proc/sys/net/ipv4/conf/all/accept_redirects

# 启用 SYN Cookie（防 SYN Flood）
echo 1 > /proc/sys/net/ipv4/tcp_syncookies
```

---

### 任务 4：应用层加固（30 分钟）

**Web 服务器加固（Nginx 示例）**：

```nginx
# 隐藏版本信息
server_tokens off;

# 安全头部
add_header X-Frame-Options "SAMEORIGIN";
add_header X-Content-Type-Options "nosniff";
add_header X-XSS-Protection "1; mode=block";
add_header Content-Security-Policy "default-src 'self'";

# 限制请求大小
client_max_body_size 10M;

# 禁用不必要的 HTTP 方法
if ($request_method !~ ^(GET|POST|HEAD)$) {
    return 405;
}
```

**数据库加固（MySQL 示例）**：

```sql
-- 删除匿名用户
DELETE FROM mysql.user WHERE User='';

-- 删除远程 root 访问
DELETE FROM mysql.user WHERE User='root' AND Host NOT IN ('localhost', '127.0.0.1', '::1');

-- 删除测试数据库
DROP DATABASE IF EXISTS test;

-- 刷新权限
FLUSH PRIVILEGES;
```

---

## ✅ 今日闯关检查

| 检查项 | 状态 |
|--------|------|
| Windows 加固清单完成 | ⬜ |
| Linux 加固清单完成 | ⬜ |
| 网络层加固理解 | ⬜ |
| 应用层加固理解 | ⬜ |

---

## 📖 底层补课（做完实验再看）

### 加固原则

1. **最小权限**：只授予必要的权限
2. **纵深防御**：多层防护
3. **默认拒绝**：默认禁止，显式允许
4. **安全基线**：建立并维护安全配置基线

### 加固参考标准

| 标准 | 来源 | 用途 |
|------|------|------|
| CIS Benchmark | CIS | 系统加固基线 |
| STIG | DISA | 美军安全配置 |
| NIST SP 800-123 | NIST | 服务器安全指南 |

---

## ⚠️ 避坑指南

| 坑点 | 症状 | 解决方案 |
|------|------|----------|
| 加固过度 | 业务中断 | 先测试，后生产 |
| 遗漏检查 | 安全隐患 | 使用 checklist |
| 配置回退 | 加固失效 | 配置管理 + 定期审计 |

---

## 💡 知识卡片速查

### 快速加固命令

| 系统 | 场景 | 命令 |
|------|------|------|
| Windows | 禁用 Guest | `net user Guest /active:no` |
| Windows | 开启审计 | `auditpol /set ...` |
| Linux | SSH 加固 | 编辑 sshd_config |
| Linux | 防火墙 | `ufw enable` |

---

## 🚀 明日预告

**Day 080：红蓝对抗 - 防御策略制定**

明天你将：
- 学习制定整体防御策略
- 了解安全运营中心（SOC）
- 理解事件响应流程

---

## 📊 学习记录（学习者填写）

| 项目 | 内容 |
|------|------|
| 实际学习日期 | ____年__月__日 |
| 实际用时 | ____ 小时 |
| 遇到的问题 | |
| 解决方法 | |
| 今日收获 | |
| 自评分数 | ⭐⭐⭐⭐⭐ (1-5星) |

---

> 💬 **导师点评**：
>
> 安全加固是防御的基础。
>
> 渗透测试发现问题只是第一步，加固才能真正提升安全性。
>
> 建议你把今天的 checklist 保存下来，在实际工作中使用和完善。
>
> 一个好的加固清单是多年经验的积累。

```bash
echo "Day 079 Complete! 安全加固 Checklist 掌握！"
```
